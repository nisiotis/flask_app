<!doctype html>
<title>Users Index Page</title>
<h1>All Users :: <a href="add">Add User</a> :: <a href="stats">User Stats</a></h1>
{% if result %}
    {{ result }} <br />
{% endif %}
{% for user in users %}
    {{ user.id }} {{ user.name }} {{ user.email }} {{ user.progress }} <br />
{% endfor %}

